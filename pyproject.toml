[project]
name = "expense-tracker-mcp-server"
version = "0.1.0"
description = "Remote MCP server for expense tracking"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastmcp>=2.13.1",
    "aiosqlite",
]

[project.scripts]
expense-tracker-mcp-server = "main:app"

